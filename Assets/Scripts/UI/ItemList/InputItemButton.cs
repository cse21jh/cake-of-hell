using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InputItemButton : MonoBehaviour
{
    /*public string listCategory;

    public int itemIndex;
    
    public void OnClickExit()
    {
        switch(listCategory)
        {
            case "Raw":
                RawInput();
                break;
            case "Base":
                BaseInput();
                break;
            case "Topping":
                ToppingInput();
                break;
            case "Icing":
                IcingInput();
                break;
        }
    }


    private void RawInput()
    {
        // 사냥꾼에서 눌렀을 때 대입 되도록 구현 필요
        Debug.Log("Can not input");
    }


    private void BaseInput()
    {
        if(UiManager.Instance.openByMaking && SaveManager.Instance.NumberOfBase[itemIndex]>0)
        {
            GameManager.Instance.inputBase = (BaseIndex)itemIndex;
            Debug.Log(GameManager.Instance.inputBase.ToString());
        }
        else
        {
            Debug.Log("Can not input");

        }
    }

    private void IcingInput()
    {
        if (UiManager.Instance.openByMaking && SaveManager.Instance.NumberOfIcing[itemIndex] > 0)
        {
            GameManager.Instance.inputIcing = (IcingIndex)itemIndex;
            Debug.Log(GameManager.Instance.inputIcing.ToString());
        }
        else
        {
            Debug.Log("Can not input");

        }
    }

    private void ToppingInput()
    {
        if (UiManager.Instance.openByMaking && SaveManager.Instance.NumberOfTopping[itemIndex] > 0)
        {
            GameManager.Instance.inputTopping = (ToppingIndex)itemIndex;
            Debug.Log(GameManager.Instance.inputTopping.ToString());
        }
        else
        {
            Debug.Log("Can not input");

        }
    }*/
}
